spring:
  application:
    name: service-0
  cloud:
    config:
      uri: http://192.168.1.41:8888
  sleuth:
    sampler:
      percentage: 1 # 0.2 is default
---
spring:
  profiles: development
  cloud:
    config:
      uri: http://localhost:8888